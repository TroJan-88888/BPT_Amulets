// google.js — legacy loader (keeps compatibility if needed)
window.addEventListener('DOMContentLoaded', async () => {
  // google gallery handled by app.js aggregate; this file kept for per-page initializations if needed
  // no-op (kept for backward compatibility)
});
